name: Routine Draft — Review Request

on:
  push:
    branches: [routine-drafts]
    paths:
      - 'O-output/W**/final-post.md'

# GITHUB_TOKEN needs these so `gh pr create` (Open PR step) can work.
# Without them the token is read-only and the workflow fails on every run.
permissions:
  contents: write
  pull-requests: write

jobs:
  open-pr-and-notify:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/routine-drafts'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Find changed week folder
        id: find_post
        run: |
          FILE=$(git diff --name-only HEAD~1 HEAD \
            | grep -E 'O-output/W[0-9]+/final/final-post.md' | head -1)

          if [ -z "$FILE" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # final-post.md lives in W[NN]/final/ — validate the WEEK folder only
          FOLDER=$(echo "$FILE" | grep -oE 'O-output/W[0-9]+')
          WEEK=$(echo "$FOLDER" | grep -oE 'W[0-9]+')

          echo "skip=false"      >> $GITHUB_OUTPUT
          echo "folder=$FOLDER"  >> $GITHUB_OUTPUT
          echo "week=$WEEK"      >> $GITHUB_OUTPUT
          echo "post_file=$FILE" >> $GITHUB_OUTPUT

      - name: Validate folder naming
        if: steps.find_post.outputs.skip == 'false'
        id: validate
        run: |
          FOLDER="${{ steps.find_post.outputs.folder }}"
          # Must match O-output/W followed by exactly 2 digits
          if echo "$FOLDER" | grep -qP '^O-output/W\d{2}$'; then
            echo "valid=true" >> $GITHUB_OUTPUT
            echo "✅ Folder name valid: $FOLDER"
          else
            echo "valid=false" >> $GITHUB_OUTPUT
            echo "❌ Invalid folder name: $FOLDER — must be O-output/W[NN]"
          fi

      - name: Open PR to main
        if: steps.find_post.outputs.skip == 'false'
        env:
          GH_TOKEN:  ${{ secrets.GITHUB_TOKEN }}
          POST_FILE: ${{ steps.find_post.outputs.post_file }}
        run: |
          WEEK="${{ steps.find_post.outputs.week }}"
          VALID="${{ steps.validate.outputs.valid }}"

          TITLE="[$WEEK] LinkedIn post — ready for review"
          if [ "$VALID" = "false" ]; then
            TITLE="⚠️ [$WEEK] LinkedIn post — FOLDER NAME INVALID"
          fi

          # The PR body is assembled with printf so that no body line sits at
          # column 0. A line at column 0 terminates this YAML block scalar and
          # makes the whole workflow file invalid.
          BODY=$(printf '%s\n' \
            "## תוכן שבועי — ממתין לאישורך" \
            "" \
            "שבוע: $WEEK" \
            "קובץ: $POST_FILE" \
            "" \
            "### לפני merge לבדוק:" \
            "- [ ] שם התיקייה בפורמט O-output/W[NN]/" \
            "- [ ] final-post.md קיים ב-final/" \
            "- [ ] שני הפוסטים עברו Gatekeeper" \
            "- [ ] visual-data.json קיים ב-process/" \
            "" \
            "Merge לmain יפעיל אוטומטית את שליחת האימייל.")

          # Check if PR already exists
          EXISTING=$(gh pr list --head routine-drafts --base main --json number --jq '.[0].number' 2>/dev/null)

          if [ -z "$EXISTING" ]; then
            gh pr create \
              --title "$TITLE" \
              --body "$BODY" \
              --base main \
              --head routine-drafts
          else
            echo "PR #$EXISTING already exists — updated with new push"
          fi

      - name: Send review notification email
        if: steps.find_post.outputs.skip == 'false' && steps.find_post.outputs.post_file != ''
        env:
          GMAIL_APP_PASS: ${{ secrets.GMAIL_APP_PASS }}
          POST_FILE:      ${{ steps.find_post.outputs.post_file }}
          WEEK:           ${{ steps.find_post.outputs.week }}
          VALID:          ${{ steps.validate.outputs.valid }}
        run: |
          python3 - <<'EOF'
          import smtplib, os
          from email.mime.text      import MIMEText
          from email.mime.multipart import MIMEMultipart

          post_file = os.environ['POST_FILE']
          week      = os.environ['WEEK']
          password  = os.environ['GMAIL_APP_PASS']
          valid     = os.environ.get('VALID', 'true')
          sender    = 'rantimor@gmail.com'

          with open(post_file, encoding='utf-8') as f:
              post_content = f.read()

          warning = ""
          if valid == "false":
            warning = "\n⚠️  שם התיקייה לא תקני — יש לתקן לפני merge!\n"

          warn_flag = ' ⚠️' if valid == 'false' else ''
          subject = f"StoreNext {week} — ממתין לאישורך{warn_flag}"

          sep = '=' * 50
          approve_instructions = '\n'.join([
              '✅ לאישור הפוסטים:',
              '  1. פתח: https://github.com/rantimor-b2biz/StoreNext/pulls',
              '  2. לחץ "Merge pull request"',
              '  3. במחשב המקומי: git checkout main && git pull origin main',
              '',
              '❌ לדחייה / עריכה: סגור את ה-PR ב-GitHub ופתח סשן חדש עם הערות.',
          ])
          body = '\n'.join([
              'שלום,',
              '',
              f'פוסטי LinkedIn שבועיים ({week}) נוצרו וממתינים לאישורך.',
              warning,
              approve_instructions,
              '',
              sep,
              '',
              post_content,
              '',
              sep,
              '',
              f'קובץ: {post_file}',
              'Branch: routine-drafts → main',
          ])

          msg = MIMEMultipart()
          msg['Subject'] = subject
          msg['From']    = sender
          msg['To']      = sender
          msg.attach(MIMEText(body, 'plain', 'utf-8'))

          with smtplib.SMTP('smtp.gmail.com', 587) as s:
              s.starttls()
              s.login(sender, password)
              s.sendmail(sender, sender, msg.as_string())

          print(f'Review email sent: {subject}')
          EOF
